# student-registration-app-
A full-stack student registration app built with Spring Boot and React, using Amazon RDS (MariaDB). Deployed in a custom AWS VPC with public/private subnets, IGW, NAT Gateway, ALB, Auto Scaling Group, and Route 53. Domain nimish.shop routes traffic to the load-balanced frontend.
